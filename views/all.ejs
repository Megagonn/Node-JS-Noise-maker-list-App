<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    
</head>
<body>
    <div>
        <div class="w-75 mx-auto border border-success p-3 my-5 rounde">
            <table class="my-2 w-100 rounded table table-striped border border-success">
                <thead>
                    <th class="p-2 mx-2 w-25 text-center">S/N</th>
                    <th class="p-2 mx-2 w-25 text-center">Name</th>
                    <th class="p-2 mx-2 w-25 text-center">Count</th>
                    <th>Actions</th>
                </thead>
                <% if (list.length > 0) { %>
                    <% for( let i = 0; i < list.length; i++ ) { %>
                        <tr>
                            <td class="p-2 mx-2 w-25 text-center"><%= i+1 %> </td>
                            <td class="p-2 mx-2 w-25 text-center"><%= list[i].name %> </td>
                            <td class="p-2 mx-2 w-25 text-center" id="count">
                                <div><%= list[i].count %></div>
                            </td>
                            <td style="display: flex;">
                                <form action="/plus" method="post" class="mx-1">
                                    <input type="hidden" name="index" value="<%= i %> ">
                                    <button class="btn btn-success" id="add">+</button>
                                </form>
                                <form action="/del" method="post" class="mx-1">
                                    <input type="hidden" name="index" value="<%= i %> ">
                                    <button class="btn btn-info" id="del">X</button>
                                </form>
                                <form action="/-" method="post" class="mx-1">
                                    <input type="hidden" name="index" value="<%= i %> ">
                                    <button class="btn btn-danger" id="minus">-</button>
                                </form>
                            </td>
                        </tr>
                        
                    <% } %>
                <% } else { %>
                    <div class="alert alert-info">Empty, add names to the list by clicking the "Add" button</div>
                <% } %> 
            </table>
            <a href="add.ejs" ><button class="w-100 btn btn-success" id="addName">Add</button></a>
        </div>
    </div>
</body>
</html>